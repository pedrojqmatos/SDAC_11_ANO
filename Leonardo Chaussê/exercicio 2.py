#resolução eu
horas = input("quantas horas trabalha por dia?:")
valor = input("qual o valor que recebe por hora trabalhada?:")
total = float(horas) * float(valor) * 30
print ("voce ira receber", total ,"€""por dia!")

#resolução professor 
nhours = input("number of hours:")
nrate = input("Rate amount:")
ntotal = float(nhours) * float(nrate)
print("ior amount is:", ntotal,)


